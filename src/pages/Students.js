// import Barchart from '../components/Barchart';

const Students = () => {
    return (
        <div>
            <h1>Students</h1>
        </div>
    )
}

// algemene / globale chart 
// show alll students = filter all = default 
// all assignments -> gemiddelde score per opdracht van alle studenten berekenen 
// per assignment de som van alle scores, delen door de hoeveelheid studenten

export default Students;