const Student = () => {
    // const { studentName } = useParams()
    // console.log(studentName);

    return (
        <div>
            <h1>Student</h1>

        </div>
    )
}

export default Student;